use core.math;

CustomType vs_main(float paramA,const float paramB,float cache) {
    float a = pow5(123);
    #ifdef HAS_POSITION
    for(int i = 0; i< 10;i++) {
        float c = i;
    }
    #else
      int no = 11;
    #endif
  
    a = cPI;
}